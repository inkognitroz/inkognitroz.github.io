# How to update content

- Edit `/public/content.json` directly, or use `/public/admin.html` in your browser.
- Keep data under `site` and `sections` keys.
- Each card supports: `title`, `description`, `link`, `tags`.
- Create a PR with content updates; once merged to `main`, GitHub Pages redeploys automatically.
