# AGENTS

Use this repository as the public hub for **SaaS Fabric** first. Prefer small PRs that update `public/content.json`, docs, and static assets.

Guardrails:
- Keep GitHub Pages compatibility.
- Publish from `/public` only.
- Keep v1 simple, static, safe, and non-technical-user friendly.
- Do not add backend requirements for v1.
- Never commit secrets.
- Keep SaaS Fabric as the repository's top-level identity. MMIR is a product/app track unless a dedicated repo/domain split is intentionally completed.
