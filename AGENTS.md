# AGENTS

Use this repository as a content hub first. Prefer small PRs that update `public/content.json`, docs, and static assets.

Guardrails:
- Keep GitHub Pages compatibility.
- Publish from `/public` only.
- Do not add backend requirements for v1.
- Never commit secrets.
